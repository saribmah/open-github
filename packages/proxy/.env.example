# Daytona Configuration
DAYTONA_API_KEY=your-api-key-here
DAYTONA_API_URL=https://api.daytona.app

# Proxy Configuration
PORT=3002
HOST=0.0.0.0              # Use 0.0.0.0 for cloud platforms (Render, Fly.io, etc.)
PROXY_DOMAIN=proxy.yourdomain.com

# Cache Configuration
CACHE_TTL=300000          # Cache TTL in milliseconds (5 minutes)
