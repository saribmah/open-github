# API Configuration
# Production: Use https://open-github.com for sandbox provisioning
# Local dev: Use http://localhost:3001 or your local worker URL
VITE_API_URL=https://open-github.com

# Fallback for local development (if not using backend)
VITE_OPENCODE_SERVER_HOST=127.0.0.1
VITE_OPENCODE_SERVER_PORT=4096
